# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env (.env.example のみ追跡)
.env
.env.*
!.env.example

# ローカル専用のエージェント設定(MCP 承認等を含む — 誤 commit すると CI の除去前提が崩れる)
.claude/settings.local.json

# misc
*.log
.DS_Store
.vercel
/coverage

# TypeScript incremental キャッシュ(古いキャッシュが削除済みファイルを参照して幻の型エラーを出す)
tsconfig.tsbuildinfo
e2e/.auth/
e2e/screenshots/
e2e/test-results/
playwright-report/
test-results/
